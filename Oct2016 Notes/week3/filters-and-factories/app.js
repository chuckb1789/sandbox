// Define our angular module in its own file.
// Since we are passing in the dependency list (an emtpy array) as a second argument, this denotes that we are declaring the module.
angular.module("SchoolApp", []);